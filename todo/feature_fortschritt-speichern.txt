Status: Plan

Bearbeitet von: --

Priorität: H


Beschreibung:

Der Fortschritt sollte beim Schließen des Tools automatisch gespeichert werden. Wenn z.B. die ersten 10 Fragen gelöst wurden und das Tool geschlossen wird, so sollte beim nächsten Start die 11. Frage angezeigt werden.